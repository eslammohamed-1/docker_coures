أول حاجة عايز أقولها بالتفصيل بس، هنرجع للأرقام الأخيرة، بس مش هطول في الحتة دي. أول حاجة عايز أقولها عن الـ Docker Engine نفسه، لما عملنا docker version، شفنا الـ Client والـ Server. الـ Docker كـ Engine، هو اللي بيعمل الـ Container وبيديره زي الـ Hyper-V مثلاً في الـ Virtual Machines أو الـ VMware. بيتكون من 3 حاجات رئيسية، أول حاجة الـ runc والـ containerd والـ Daemon، هناخدهم واحدة واحدة. دلوقتي يعني إيه Daemon؟ الـ Docker Daemon أو اسمه dockerd في Linux، ده الـ Engine، الـ Server-side بتاع Docker. ده وظيفته إنه يخلي الـ Docker يشتغل بشكل سليم. لأن لما بنتكلم عن الـ Containers، إحنا مقلناش إن الـ Container محتاج بس الـ Runtime عشان يتعامل مع نظام التشغيل. ويبدأ يكمل عليه، اللي بيوصل الـ Container لبعضها وبيتحكم فيها هو الـ Docker Daemon. الـ Daemon برضه هو اللي بيستقبل الـ Commands، زي ما شفنا لما عملنا docker image pull، و docker container run، و create، و start، و exec. كل الـ Commands دي بتيجي من الـ Docker Client. وبتروح للـ Daemon. وهو بيشوف إنت محتاج إيه، محتاج Image، ولا Network، ولا User، ولا تكريت Container. وبينفذ على الأساس ده. لو الطلب ليه علاقة بإنك تشغل Container، فاللي بيحصل إن الـ Daemon بيباصي الـ Request للـ containerd. الـ containerd هو عبارة عن Component، ووظيفته إدارة أي حاجة ليها علاقة بالـ Container. لو هتصنع Container من أول وجديد، فاللي بيحصل إن الـ containerd هو اللي بيبدأ العملية. الـ runc هو وقت الـ execution للـ container، كل container شغال بيبقى عبارة عن process runc. والـ containerd وظيفتها تعمل الـ lifecycle management زي الـ start والـ stop والـ pause وحاجات زي كده. ده بشكل عام. فإنت في الأول مثلاً بتكتب أي Command. docker container run وشوية switches. وتدوس Enter. اللي بيحصل إن الكلام ده بيروح للـ Docker Daemon. الـ Docker Daemon بياخد الـ request ويباصيه للـ containerd. الـ containerd ده هو اللي وظيفته إنه يجهز الـ container. وبعدين يبعت الـ container ده للـ runc. الـ runc بيشوف فين الـ Image اللي المفروض يشغل الـ container على أساسها. وبيعمل container تاني بـ runc تانية وهكذا. يبقى الـ container ده قايم. لأن بيبقى عندي containerd واحدة بس. جوه الـ Docker Host. أو الـ machine اللي نازل عليها الـ Docker Engine نفسه. وعندي Docker Daemon واحد بس اللي هو dockerd. ولكل container شغال. هتلاقي عندك الـ runc. اللي هي المسؤولة عن الـ container ده. وده بيتسمى Loosely. Coupled Architecture. إنه مش الـ Daemon هو اللي بيعمل كل حاجة في كل حتة ولا الـ containerd هي اللي بتعمل كل حاجة في كل حتة ولا الـ runc هي اللي بتعمل كل حاجة في كل حتة يبقى ده الـ Architecture بشكل عام مع كل Container بيتكاريت لما تكاريت الـ Container بتشتغل الـ runc الـ runc دي كـ Process دايماً Running علشان ما تبقاش مرتبطة بالـ Container وعشان الـ Container ما يبقاش مرتبط بالـ containerd ولا يبقى مرتبط بالـ Docker Daemon نفسه بحيث إنك ممكن تعمل Upgrade للـ Daemon، تعمل Start أو Stop للـ Daemon نفسه ويفضل الـ Container شغال و Up & Running دايماً بيبقى فيه Process اسمها shim دي عبارة عن Process فيها شوية من الـ containerd وارثة شوية حاجات من الـ containerd وشوية حاجات من الـ runc بتفضل دايماً قايمة مع الـ Container تخليك إنك أياً كان اللي حصل في الـ containerd عملت Start عملت Stop عملت Upgrade ده ما يأثرش على الـ Container اللي شغال فتقلل اعتمادية الـ Container على الـ runc وعلى الـ containerd وعلى الـ Docker Daemon نفسه لو إنت فورت الـ Docker Engine كله ونزلت Docker من أول وجديد هتلاقي الـ Container شغالة و Up and Running عادي جداً وما اتأثرتش فده الـ Engine نفسه علشان تشوف الـ Engine دي عندك أكتر من طريقة ممكن تشوف الـ Process الـ Running وتشوف الـ containerd وتشوف الـ Docker تعال أوريك مثال على ده هنروح على الـ Machine اللي عليها الـ Docker أو اللي اسمها Docker Host وهاجي أقول له مثلاً ps -lef وأقول له هات لي أي حاجة عليها كلمة فيها Docker نشوف الـ processes الـ running هنجرب كده فهيوريني إن فيه process running اللي هي dockerd تمام؟ ده الـ Docker Daemon لو بالمثل برضه قلت له وريني مثلاً الـ container فهيوريني إن فيه container دي running تمام؟ فده الـ Docker Daemon ودي الـ container دي بالمثل برضه لما نيجي نشغل container برضه أو تشوف الـ process اللي هي running أو الـ shim اللي running لكل container من الـ containers اللي أنت شغال عليها يبقى ده كان الـ structure بشكل عام مثال على ذلك إنت مثلاً عملت docker container run command أو قلت له مثلاً docker container run alpine أو python أو mongo، أي حاجة اللي هيحصل إن الـ command ده هيروح للـ API بتاع الـ Docker اللي موجود في الـ Docker Daemon الـ Docker Daemon هيشوف المفروض إيه اللي يتعمل اللي إنت طلبته مثلاً كنت بتـ create container والـ container ده هيرن مثلاً بـ switches معينة بيشوف هل الـ image موجودة عندك locally ولا مش عندك لو الـ image موجودة locally عندك هيبدأ إنه يباصي الكلام ده للـ containerd اللي هيكاريت الـ container بناءً على الـ image اللي موجودة locally لو الـ image مش موجودة locally هيضطر الـ dockerd قبل ما يعمل الكلام ده يعمل download للـ image يعمل pull للـ image من الـ Docker Hub مثلاً تمام؟ بعد كده الـ containerd هيباصي الـ request ده للـ runc والـ runc هي اللي فعلاً هتبدأ تبني الـ container من الـ image اللي موجودة أو اللي اتعمل لها pull أوكي؟ فده بشكل عام وزي ما إحنا قلنا الـ benefit إن الـ container runtime يبقى decoupled decoupled من الـ Docker daemon نفسه أوكي؟ ده حتى بيسموه daemon-less containers يعني ممكن الـ container يشتغل والـ daemon بتعمله upgrade أو أي حاجة ثانية أوكي؟ الـ API أو الـ Docker daemon بيلسن على /var/run/docker.sock تمام؟ لما تلاقي ده Running عندك لما الـ Docker ياخد Access لده أو الـ container ياخد Access لده علشان يقدر يباصي الـ client الـ Docker client يباصي الـ commands للـ Docker أوكي؟ By default الـ Docker daemon بيلسن على Port 2375 TCP Port وبيلسن على HTTP يعني مش Secure دي Security issue موجودة في الـ Docker عشان تتجاوز الـ Security issues دي تقدر تشتغل بالـ TLS وتستخدم Certificate سواء كانت Self-signed certificate أو CA certificate هسيب لك الخطوات هنا وهسيب لك Link ليها برضه عشان تشوف الخطوات بالترتيب تقدر تعمل Configure للـ Docker إنه ميسلنش على HTTP بيلسن على HTTPS مثلاً أوكي؟ فأنا هسيب لك الخطوات دي هنا في الـ Note الفرق ده هيفرق كتير مع الناس اللي حياتها كلها الـ Containers في الـ Production وإحنا محتاجينه لأسباب تانية فده كان الـ Docker engine بشكل عام